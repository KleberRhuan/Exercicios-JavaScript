function isPair(field) {

  if (field % 2 === 0) {
    return true;
  }
  return false;
}

function choiceValidation(valid) {

  if (valid === "Par" || valid === "Impar"){
    return true;
  }
  return false;
}